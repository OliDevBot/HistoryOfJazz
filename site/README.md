Hugo site for HistoryOfJazz (minimal theme)

Preview locally: install Hugo (https://gohugo.io/) and run:
  hugo server -s site

Build locally:
  hugo -s site -d public

This site expects posts in the top-level repo's content/published/ as .md files with frontmatter.
